# Claude Code 開発プロンプト集（競輪予想アプリ）

前提：`01_競輪予想アプリ_仕様会議まとめ.md` をプロジェクトフォルダに置き、Claude Codeに読み込ませてから各プロンプトを順番に投げてください。フェーズごとに動作確認しながら進めるのがおすすめです。

---

## Phase 0: プロジェクト初期化＋PWA設定

```
「01_競輪予想アプリ_仕様会議まとめ.md」の設計に沿って、競輪予想アプリのプロジェクトを初期化してください。

要件：
- Next.js（App Router）＋ TypeScript ＋ Tailwind CSSでセットアップ
- PWA対応にして、iPhoneのSafariで「ホーム画面に追加」した際にアプリのように動作するようにする（manifest.json、アイコン、next-pwa等の設定）
- フォルダ構成は機能ごとに分かりやすく分割する（例：app/, lib/, components/, scraper/, db/）
- READMEに開発の進め方とローカル起動手順をまとめる

まずは「Hello World」レベルでよいので、PWAとして正しく動作する最小構成を作ってください。
```

---

## Phase 1: DB設計＋スクレイピングモジュール

```
競輪予想アプリのデータ層を実装してください。

要件：
- DBはSQLiteを使用。以下のテーブルを設計する：
  - races（レース情報：開催場、日時、レース番号、距離、バンク特性など）
  - racers（選手情報：氏名、級班、脚質、ギア倍数、地区など）
  - entries（出走表：race_idとracer_idの紐付け、車番、想定ライン情報）
  - results（過去の着順結果、統計計算に使う）
  - odds（オッズの推移データ）
- Pythonで公式サイト等から出走表・選手データ・オッズを取得するスクレイピングスクリプトを作成する
  - 対象サイトのURL構造は一緒に確認しながら実装したいので、まず1レース分のサンプル取得ロジックを提案してください
  - robots.txtや利用規約に配慮し、アクセス間隔を空けるなど節度のある作りにする
- 取得したデータをDBに保存する処理まで作る

まず1レース分だけ、手動実行でデータが取れてDBに入るところまでを目標にしてください。
```

---

## Phase 2: 予想ロジック実装（3本柱スコアリング）

```
「01_競輪予想アプリ_仕様会議まとめ.md」の「3. 予想ロジック設計」に基づいて、予想スコアリングロジックを実装してください。

要件：
- 選手ごとに以下3つのスコアを算出する関数を作る
  1. ラインスコア（隊列構成・位置・連携の強さから算出）
  2. 脚質実力スコア（脚質とバンク相性、級班、勝率・連対率、ギア倍数から算出）
  3. データ統計スコア（バンク特性、天候・風向、出走間隔、過去の同条件成績、オッズの動きから算出）
- 3つのスコアを重み付け合成して総合スコアを出し、上位から ◎○▲△× を自動付与する
- 重み(w1, w2, w3)は設定ファイルまたはDBで後から調整できるようにする
- 総合スコア上位選手から3連単フォーメーション・3連複ボックスなどの買い目候補を自動生成する関数も作る
- 各スコアの算出根拠（どの要素が効いたか）が後で確認できるよう、内訳もあわせて返すようにする

まずはPhase1で取得したサンプルデータ1レース分に対して、スコアと印が正しく出力されることを確認してください。
```

---

## Phase 3: UI実装

```
「01_競輪予想アプリ_仕様会議まとめ.md」の「4. 画面構成」に基づいて、以下の画面をNext.jsで実装してください。

- レース一覧画面（本日/開催中のレースを表示）
- 出走表・予想画面（選手ごとにライン/脚質/統計/総合スコアと印を表示。番手・3番手なども視覚的にわかるように）
- 選手データ画面（個別選手の過去成績・脚質・ギア比などの詳細）
- 買い目提案画面（Phase2で生成した買い目候補を見やすく表示）

デザインはモバイル（iPhone）での閲覧を最優先にしたレスポンシブデザインにしてください。Tailwind CSSを使い、競輪らしい見やすさ・視認性を重視した配色にしてください。
```

---

## Phase 4: 設定・重み調整・精度検証機能

```
以下の機能を追加実装してください。

- 設定画面：ライン/脚質/統計の重み(w1,w2,w3)をスライダー等で調整できるようにする。調整結果は即座に予想画面に反映される
- 予想結果と実際の着順を記録し、的中率・回収率を後から振り返れる簡易的な検証機能（自分の予想精度を上げるためのフィードバックループ）
- 過去レースの予想と結果を一覧で見返せる履歴画面
```

---

## Phase 5: デプロイ・iPhone動作確認

```
このアプリを個人利用できる形でデプロイしてください。

要件：
- フロントエンド（Next.js）はVercelにデプロイできる状態にする
- スクレイピングバッチは定期実行（例：GitHub Actionsのスケジュール実行、または自分のPC/サーバーでのcron）で回せるようにし、その設定手順をREADMEにまとめる
- 本番URLにiPhoneのSafariでアクセスし、「ホーム画面に追加」した際に正しくPWAとして動作するか確認する手順もREADMEに書く
- 環境変数やAPIキー等の管理方法（.envの扱い）も明記する
```

---

## 使い方のコツ

- 各Phaseは一気に投げず、動作確認 → 次のPhaseの順で進めると手戻りが少ないです。
- スクレイピング対象サイトのHTML構造が変わることがあるので、Phase1で「動かなくなったら教えてください」と伝えておくと、Claude Codeが調整しやすくなります。
- 重み(w1,w2,w3)は最初から正解を決めず、実際にレースを予想してみて振り返りながら微調整していくのがおすすめです。
