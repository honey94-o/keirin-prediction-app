# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# python / scraper
scraper/.venv/
scraper/__pycache__/
*.pyc

# sqlite
db/*.db
db/*.db-journal
db/backup/

# scripts/fetch-venue-weather.tsのキャッシュ（大きい・日付範囲固定のスナップショット、
# 再実行すればいつでも作り直せるため）。venue_coords.jsonは軽量かつ再利用価値があるため追跡する。
scripts/data/venue_weather.json

.vercel
.env*

.env*
